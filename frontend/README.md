# Nestora frontend

Premium rental-lifecycle prototype.

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:3000

Demo data is labelled throughout. Authentication, uploads, and payments
are local simulations. See `../docs/ARCHITECTURE.md`.
